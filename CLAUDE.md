# CLAUDE.md — co-secondbrain

이 저장소에서 작업하는 에이전트의 행동 규칙.

§1~§4는 [multica-ai/andrej-karpathy-skills](https://github.com/multica-ai/andrej-karpathy-skills)
(MIT, forrestchang) 의 `CLAUDE.md` 를 그대로 가져온 것이다. Andrej Karpathy의
[LLM 코딩 함정 관찰](https://x.com/karpathy/status/2015883857489522876)에서 파생됐다.
§5 이후는 이 프로젝트 고유 규칙이다.

**트레이드오프:** 이 지침은 속도보다 신중함으로 기운다. 사소한 작업에는 판단해서 적용한다.

---

## 1. 코딩 전에 생각한다

**넘겨짚지 않는다. 혼란을 숨기지 않는다. 트레이드오프를 드러낸다.**

구현하기 전에:
- 가정을 명시한다. 불확실하면 묻는다.
- 해석이 여러 개면 전부 제시한다 — 조용히 하나를 고르지 않는다.
- 더 단순한 방법이 있으면 말한다. 근거가 있으면 반대한다.
- 불명확하면 멈춘다. 무엇이 혼란스러운지 이름 붙이고 묻는다.

## 2. 단순함이 먼저다

**문제를 푸는 최소한의 코드. 예측성 코드는 쓰지 않는다.**

- 요청받지 않은 기능은 넣지 않는다.
- 한 번만 쓰는 코드에 추상화를 만들지 않는다.
- 요청되지 않은 "유연성"·"설정 가능성"을 넣지 않는다.
- 일어날 수 없는 상황에 오류 처리를 넣지 않는다.
- 200줄을 썼는데 50줄로 될 것 같으면 다시 쓴다.

자문한다: "시니어 엔지니어가 이걸 과설계라고 할까?" 그렇다면 단순화한다.

## 3. 외과적 변경

**건드려야 하는 것만 건드린다. 자기가 만든 것만 치운다.**

기존 코드를 고칠 때:
- 인접 코드·주석·서식을 "개선"하지 않는다.
- 망가지지 않은 것을 리팩터링하지 않는다.
- 자기 취향과 달라도 기존 스타일에 맞춘다.
- 무관한 죽은 코드를 발견하면 **말하되 지우지 않는다.**

자기 변경이 고아를 만들었을 때:
- **자기 변경 때문에** 안 쓰이게 된 import·변수·함수를 제거한다.
- 원래 있던 죽은 코드는 요청받지 않는 한 지우지 않는다.

판정 기준: 바뀐 모든 줄이 사용자 요청으로 직접 추적돼야 한다.

## 4. 목표 주도 실행

**성공 기준을 정의한다. 검증될 때까지 돈다.**

작업을 검증 가능한 목표로 바꾼다:
- "검증 추가" → "잘못된 입력에 대한 테스트를 쓰고, 통과시킨다"
- "버그 수정" → "재현하는 테스트를 쓰고, 통과시킨다"
- "X 리팩터링" → "전후로 테스트가 통과하는지 확인한다"

여러 단계 작업이면 짧은 계획을 먼저 말한다:

```
1. [단계] → 검증: [확인 방법]
2. [단계] → 검증: [확인 방법]
```

강한 성공 기준이 있으면 혼자 루프를 돌 수 있다. 약한 기준("돌아가게 해줘")은
계속 되묻게 만든다.

---

# 이 프로젝트 고유 규칙

## 5. 추측하지 말고 측정한다

이 프로젝트의 M0 스파이크에서 **측정이 계획을 세 번 뒤집었다**
([`docs/M0-RESULTS.md`](docs/M0-RESULTS.md)). 그래서 이 규칙이 §1보다 앞선다.

- 라이브러리·플래그·API 동작을 **모르면 적지 않는다.** 실제로 돌려보거나 `--help`를 읽는다.
- 다른 프로젝트에서 가져온 파라미터는 **우리 환경에서 재검증한다.**
  실제로 두 번 틀렸다 — Jaro-Winkler 임계 0.92는 한국어에서 오병합 3건,
  엔트로피 게이트는 길이 하한이 없으면 한국어에서 뚫린다.
- 검증하지 못한 것은 문서에 **"확실하지 않음" / "추측입니다"** 로 표시한다.
  표시 없이 단정하지 않는다.
- 표본이 작으면 크기를 밝힌다. `n=1`은 통계가 아니라 일화다.

## 6. 한국어로 쓴다

- 사용자 대상 산문(문서·주석·커밋 메시지·로그)은 **한국어**.
- 코드 식별자·타입명·파일명은 영문.
- 산문 품질은 `spikes/korean/detect.mjs` 의 규칙을 따른다:
  번역투("~을 가지고 있다"), 이중 피동, 분열문("핵심은 ~다"),
  부정 대구("A가 아니라 B"), 연결어미 뒤 쉼표, 결산 lexicon("결론적으로") 회피.
- **이모지 금지** (업무 문서 · `docs/DESIGN-SYSTEM.md`와 동일).

## 7. 한국어 정규식 함정

전부 이 프로젝트에서 실제로 틀려서 발견한 것들이다.

- `\b`는 **한글 경계에서 동작하지 않는다.** 문장 종결 앵커(`[.\n]`)를 쓴다.
- **조사 이형태** — 받침 유무로 은/는, 이/가, 을/를이 갈린다.
  `핵심은` / `문제는` 을 둘 다 받아야 한다.
- 문자 n-gram 유사도는 **라틴 문자 기준 설계**다. 한국어에서 임계값을 재보정한다.
- 절 경계를 넘는 span은 정상 문장을 오탐한다. 연결어미를 부정 전방탐색으로 막는다.

## 8. 문서가 산출물이다

이 저장소는 **아직 구현 코드가 없다.** 산출물은 계획 문서와 검증 코드다.

- 설계를 바꾸면 **근거를 같이 적는다.** "왜 바꿨는지"가 없는 변경은 나중에 되돌려진다.
- 차용한 설계는 **출처와 라이선스**를 명시한다.
- 문서 간 상호 참조는 절 번호까지 (`PLAN.md` §7.2).
- 계획 버전(`v0.6`)은 설계가 바뀔 때만 올린다.

## 9. 스파이크 코드 규약

`spikes/` 아래 코드는 제품 코드가 아니라 **증거**다.

- 각 스크립트는 **정답을 아는 입력**으로 돌려 PASS/FAIL을 스스로 판정한다.
- 오탐(false positive)이 0이어야 하는 검사는 그것을 **종료 코드로 강제**한다.
- 합성 데이터의 한계를 스크립트 주석과 결과 문서에 적는다.
- 측정 결과가 기대와 다르면 **결과를 적는다.** 기대에 맞게 규칙을 고치지 않는다.

## 10. 커밋

- 제목은 한국어 한 줄. 무엇을 왜 바꿨는지.
- 본문에 **측정 수치**가 있으면 적는다. "성능 개선"이 아니라 "8턴 → 2턴, $0.176 → $0.130".
- 실측으로 확인한 것과 추측을 본문에서 구분한다.
- 모델 식별자를 커밋 메시지·PR·코드 주석에 넣지 않는다.

---

## 프로젝트 맥락 (빠른 참조)

| 문서 | 내용 |
|---|---|
| [`docs/PLAN.md`](docs/PLAN.md) | 전체 계획. 여기서 시작 |
| [`docs/M0-RESULTS.md`](docs/M0-RESULTS.md) | 실측 결과. 설계 근거의 대부분 |
| [`docs/M1-NOTES.md`](docs/M1-NOTES.md) | M1 구현에서 코드를 고치게 만든 측정 |
| [`docs/HUB.md`](docs/HUB.md) | 사내 동기화 서버 |
| [`docs/REVIEW-graphify.md`](docs/REVIEW-graphify.md) · [`docs/REVIEW-imnotai.md`](docs/REVIEW-imnotai.md) | 선행 사례 검토 |
| [`spikes/`](spikes/) | 검증 코드 (실행 가능) |

**핵심 설계 원칙 3가지** (어기면 설계가 무너진다):

1. **LLM은 디스크에 직접 쓰지 않는다.** ChangeSet을 내고 사람이 diff로 승인하면 앱이 적용한다.
2. **모든 주장에 앵커 인용.** `[^src-kickoff#slide-12]` 를 클릭하면 원문 위치로 점프한다.
3. **공짜로 뽑을 수 있는 구조는 LLM에게 묻지 않는다.** 이메일 스레드는 `In-Reply-To`로,
   엑셀 의존 관계는 수식의 셀 참조로, 고아 페이지는 링크 그래프로 계산한다.
